# anotacoes do projeto (pra eu nao esquecer)

- login usa supabase auth + cookie que eu assino no sessao.ts
- upload de imagem precisa dos buckets no supabase: banners, noticias, projetos
- formulario de contato ainda nao salva (fazer tabela mensagens depois?)
- slug so gera na criacao, na edicao nao muda
- revalidatePath depois de salvar no admin

comandos que uso:
- npm run dev
- npm run db:seed (se banco vazio)
- npx prisma studio (ver tabelas)
